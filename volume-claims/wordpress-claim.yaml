apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: wordpress-claim
  namespace: clo4
spec:
  storageClassName: manual
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 10Gi
